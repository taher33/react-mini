export function jsx(tag, props) {
  // console.log({ tag, props });

  return {
    tag,
    props,
  };
}
export function jsxs(tag, props) {
  // console.log({ tag, props });

  return {
    tag,
    props,
  };
}
